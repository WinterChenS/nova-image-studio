package com.nova.studio;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Nova Image Studio backend — Spring Boot skeleton (M0 spike, WIN-10).
 *
 * <p>Version baseline under verification: Spring Boot 4.1.0 + Spring AI 2.0.0 + JDK 21.
 */
@SpringBootApplication
public class NovaStudioApplication {

    public static void main(String[] args) {
        SpringApplication.run(NovaStudioApplication.class, args);
    }
}
